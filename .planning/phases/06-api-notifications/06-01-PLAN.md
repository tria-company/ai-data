---
phase: 06-api-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/queue.ts
  - app/api/scrape/route.ts
  - app/api/jobs/route.ts
  - app/api/jobs/[id]/route.ts
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "POST /api/scrape returns 202 Accepted with jobIds array"
    - "GET /api/jobs/:id returns job status, progress, result, and timestamps"
    - "GET /api/jobs returns filtered list by projetoId and status query params"
    - "Completed jobs auto-expire from Redis after 24 hours"
  artifacts:
    - path: "app/api/scrape/route.ts"
      provides: "Async 202 scrape endpoint replacing SSE"
      contains: "profileScrapeQueue.add"
    - path: "app/api/jobs/[id]/route.ts"
      provides: "Single job status query"
      exports: ["GET"]
    - path: "app/api/jobs/route.ts"
      provides: "Job list with filters"
      exports: ["GET"]
    - path: "lib/queue.ts"
      provides: "Queue definitions with TTL defaults"
      contains: "removeOnComplete"
  key_links:
    - from: "app/api/scrape/route.ts"
      to: "lib/queue.ts"
      via: "profileScrapeQueue.add"
      pattern: "profileScrapeQueue\\.add"
    - from: "app/api/jobs/[id]/route.ts"
      to: "lib/queue.ts"
      via: "profileScrapeQueue.getJob"
      pattern: "profileScrapeQueue\\.getJob"
    - from: "app/api/jobs/route.ts"
      to: "lib/queue.ts"
      via: "profileScrapeQueue.getJobs"
      pattern: "profileScrapeQueue\\.getJobs"
---

<objective>
Replace the synchronous SSE `/api/scrape` endpoint with an async 202 pattern backed by BullMQ, and create job status/listing endpoints.

Purpose: External consumers can trigger scraping via REST API and monitor job progress without holding open connections.
Output: Three API routes (POST /api/scrape, GET /api/jobs/:id, GET /api/jobs) and queue TTL configuration.
</objective>

<execution_context>
@/Users/igorvboas/Documents/TRIA/ai-data/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igorvboas/Documents/TRIA/ai-data/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-api-notifications/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/queue.ts:
```typescript
import { Queue } from 'bullmq';
export const profileScrapeQueue = new Queue('profile-scrape', { connection });
export const postDetailsQueue = new Queue('post-details', { connection });
export { connection };
```

From workers/profile-worker.ts (job data shape):
```typescript
interface ProfileJobData {
  username: string;
  projetoId: string | null;
  maxPosts?: number;
}
```

From app/api/scrape/route.ts (current payload - same structure reused):
```typescript
const { accountId, targetUsernames, projetoId } = body;
// accountId: string (optional in new version per D-01)
// targetUsernames: string[]
// projetoId: string | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add queue TTL defaults and replace /api/scrape with async 202 endpoint</name>
  <files>lib/queue.ts, app/api/scrape/route.ts</files>
  <read_first>
    - lib/queue.ts
    - app/api/scrape/route.ts
    - workers/profile-worker.ts (lines 18-22 for ProfileJobData interface)
  </read_first>
  <action>
**lib/queue.ts** — Add `defaultJobOptions` to both queues per D-07:
- `removeOnComplete: { age: 86400, count: 200 }` (24h or 200 completed jobs max)
- `removeOnFail: { age: 604800, count: 500 }` (7 days for failed jobs)
- Keep existing `connection` definition unchanged
- Export both queues and connection as before

**app/api/scrape/route.ts** — Full replacement per D-01 and D-09:
- Remove the entire SSE/streaming implementation (ReadableStream, scrapeAccounts import, maxDuration)
- Import `profileScrapeQueue` from `@/lib/queue`
- Accept same payload `{ accountId, targetUsernames, projetoId }`
- `accountId` is now OPTIONAL (not required) — per D-01, if omitted worker auto-selects
- Validate: `targetUsernames` must be a non-empty array of strings. Return 400 with `{ error: 'targetUsernames must be a non-empty array' }` if invalid
- Enqueue one job per target username using `profileScrapeQueue.add('profile-scrape', { username, projetoId: projetoId || null, accountId: accountId || null })`
- Return 202 with `{ jobIds: string[], status: 'queued', count: number }`
- Remove `maxDuration` export (not needed for async)
  </action>
  <verify>
    <automated>cd /Users/igorvboas/Documents/TRIA/ai-data && npx tsc --noEmit app/api/scrape/route.ts lib/queue.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/queue.ts contains `removeOnComplete` with `age: 86400`
    - lib/queue.ts contains `removeOnFail` with `age: 604800`
    - app/api/scrape/route.ts imports `profileScrapeQueue` from `@/lib/queue`
    - app/api/scrape/route.ts does NOT import `scrapeAccounts`
    - app/api/scrape/route.ts does NOT contain `ReadableStream`
    - app/api/scrape/route.ts returns `status: 202`
    - app/api/scrape/route.ts contains `profileScrapeQueue.add`
    - Response body includes `jobIds`, `status: 'queued'`, and `count`
  </acceptance_criteria>
  <done>lib/queue.ts has TTL defaults, /api/scrape returns 202 with jobIds instead of SSE stream</done>
</task>

<task type="auto">
  <name>Task 2: Create GET /api/jobs/[id] and GET /api/jobs endpoints</name>
  <files>app/api/jobs/[id]/route.ts, app/api/jobs/route.ts</files>
  <read_first>
    - lib/queue.ts
    - app/api/projetos/list/route.ts (for existing API route pattern reference)
  </read_first>
  <action>
**app/api/jobs/[id]/route.ts** — Single job status per D-02:
- Import `profileScrapeQueue` from `@/lib/queue`
- Export async `GET` handler with Next.js 15+ async params: `{ params }: { params: Promise<{ id: string }> }`
- Await params: `const { id } = await params;`
- Get job: `const job = await profileScrapeQueue.getJob(id);`
- If no job, return 404 with `{ error: 'Job not found' }`
- Get state: `const state = await job.getState();`
- Return JSON with fields per D-02:
  - `jobId`: job.id
  - `status`: state (queued|active|completed|failed|delayed)
  - `progress`: job.progress (will be object `{ percent, stage }` per D-08 after worker update, or number for legacy)
  - `result`: job.returnvalue (when completed)
  - `error`: job.failedReason || null
  - `createdAt`: `new Date(job.timestamp).toISOString()`
  - `finishedAt`: job.finishedOn ? `new Date(job.finishedOn).toISOString()` : null
  - `data`: job.data (includes username, projetoId)

**app/api/jobs/route.ts** — Job list with filters per D-03:
- Import `profileScrapeQueue` from `@/lib/queue`
- Export async `GET` handler
- Parse query params: `projetoId` and `status` from `new URL(request.url).searchParams`
- Map status param to BullMQ types: if status provided use `[status]`, otherwise use `['waiting', 'active', 'completed', 'failed', 'delayed']`
- Get jobs: `profileScrapeQueue.getJobs(types, 0, 99)` (limit 100 per research recommendation)
- For each job, build response object with: `jobId`, `status` (from the type bucket it came from — do NOT call `job.getState()` per Pitfall 4), `progress`, `username` (from job.data), `projetoId` (from job.data), `createdAt`, `finishedAt`
- OPTIMIZATION per research Pitfall 4: When listing by type, infer state from the type parameter instead of calling `job.getState()` per-job. When listing ALL types, use `Promise.all` with `job.getState()` calls.
- Filter by projetoId client-side: `results.filter(j => j.projetoId === projetoId)` if param provided
- Return JSON array
  </action>
  <verify>
    <automated>cd /Users/igorvboas/Documents/TRIA/ai-data && npx tsc --noEmit app/api/jobs/route.ts app/api/jobs/\\[id\\]/route.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/api/jobs/[id]/route.ts exports a `GET` function
    - app/api/jobs/[id]/route.ts contains `await params` (Next.js 15+ async pattern)
    - app/api/jobs/[id]/route.ts contains `profileScrapeQueue.getJob`
    - app/api/jobs/[id]/route.ts returns `jobId`, `status`, `progress`, `result`, `error`, `createdAt`, `finishedAt`
    - app/api/jobs/route.ts exports a `GET` function
    - app/api/jobs/route.ts contains `profileScrapeQueue.getJobs`
    - app/api/jobs/route.ts reads `projetoId` and `status` from searchParams
    - app/api/jobs/route.ts filters by projetoId when provided
  </acceptance_criteria>
  <done>GET /api/jobs/:id returns full job status per D-02, GET /api/jobs returns filtered list per D-03</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified/created files
- `app/api/scrape/route.ts` no longer contains SSE/streaming code
- `lib/queue.ts` has TTL configuration for completed and failed jobs
- All three API routes import from `@/lib/queue` singleton (no new Queue instances)
</verification>

<success_criteria>
- POST /api/scrape accepts `{ targetUsernames, projetoId, accountId? }` and returns 202 with `{ jobIds, status: 'queued', count }`
- GET /api/jobs/:id returns job status, progress, result, error, timestamps per D-02
- GET /api/jobs returns filtered list by projetoId and status per D-03
- Queue TTL configured: 24h for completed, 7 days for failed per D-07
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-notifications/06-01-SUMMARY.md`
</output>
